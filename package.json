{
  "name": "aerodrive-zenith",
  "version": "0.3.0",
  "private": true,
  "description": "Vercel-ready offline-first browser-native driving simulator milestone for AeroDrive Zenith.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "test:security": "node scripts/security-seed-tests.mjs",
    "test:offline": "node scripts/offline-cache-audit.mjs",
    "check": "npm run typecheck && npm run test:security && npm run test:offline && npm run test:enterprise",
    "test:enterprise": "node scripts/enterprise-audit.mjs"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
